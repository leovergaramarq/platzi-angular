import { StockDirective } from './stock.directive';

describe('StockDirective', () => {
  it('should create an instance', () => {
    const directive = new StockDirective();
    expect(directive).toBeTruthy();
  });
});
